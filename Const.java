import java.awt.Color;

public class Const {
    final int WINDOW_WIDTH = 1920;
    final int WINDOW_HEIGHT = 1080;

    final int BLOCK_WIDTH = 10;
    final int BLOCK_HEIGHT = 10;

    // for NOW
    final int MAX_COLUMNS = 100;

    // green
    final Color GREEN = new Color(9,86,40);
    // french grey
    final Color HONEYDEW = new Color(92,95,89);
    final Color BLACK = new Color(0, 7, 5);

}
